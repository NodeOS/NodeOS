{
  "name": "nodeos-initramfs",
  "version": "0.0.0",
  "description": "The included script generate a initramfs image that boots a NodeOS rootfs given as a ROOT parameter on boot time. If it fails to do so, it boots to a Node.js [REPL](http://nodejs.org/api/repl.html) prompt.",
  "scripts": {
    "dockerBuild": "docker build -t initramfs .",
    "install": "scripts/build",
    "start": "scripts/start",
    "preinstall": "scripts/preinstall",
    "test": "scripts/test",
    "unbuild": "scripts/unbuild"
  },
  "author": "Jesús Leganés Combarro \"piranna\"",
  "license": "MIT",
  "peerDependencies": {
    "nodeos-barebones": "^0.0.0"
  },
  "dependencies": {
    "download-manager": "^0.0.15"
  },
  "nodeosDependencies": {
    "nodeos-mount-filesystems": "^0.1.1",
    "nodeos-mount-utils": "^0.1.0",
    "usrbinenv": "^0.0.2"
  },
  "engines": {
    "node": "<5"
  }
}
