#!/usr/bin/env bash

# [ToDo] include test for Node.js binary without virtual machine
# LD_LIBRARY_PATH=lib lib/libc.so bin/node

GRN="\e[32m"
CLR="\e[0m"

#
# Check basic functions (boot and Node.js REPL) works as expected
#

npm install jprichardson/node-suppose

scripts/test || exit $?


echo -e "${GRN}Successfully built barebones${CLR}"
