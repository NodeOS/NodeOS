containers:
  barebones:
    setup:
    - !Tar
      url: http://localhost:8080/out/nocona/barebones.tar

commands:
  run: !Command
    description: Start NodeOS barebones layer
    container: barebones
    pid1mode: exec
    run: [/init]
