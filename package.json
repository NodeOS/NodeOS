{
  "name": "NodeOS",
  "version": "0.0.0",
  "description": "Lightweight operating system using Node.js as userspace",
  "bundleDependencies": [
    "nodeos-barebones",
    "nodeos-initramfs",
    "nodeos-rootfs",
    "nodeos-usersfs"
  ],
  "scripts": {
    "BigRedButton": "scripts/BigRedButton",
    "docker": "docker run -t -i --cap-add SYS_ADMIN --device /dev/fuse -v `pwd`/out/latest/usersfs:/dev/sdb2 NodeOS",
    "dockerBuild": "scripts/dockerBuild",
    "install": "scripts/build",
    "postinstall": "scripts/postinstall",
    "publish": "scripts/publish",
    "start": "scripts/start",
    "test": "scripts/test",
    "unbuild": "scripts/unbuild"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NodeOS/NodeOS.git"
  },
  "keywords": [
    "OS",
    "Linux",
    "npm"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/NodeOS/NodeOS/issues"
  },
  "homepage": "http://node-os.com",
  "private": true,
  "dependencies": {
    "cpio2tar": "0.0.1",
    "debugfs": "^0.0.2",
    "download-manager": "^0.0.15",
    "fs-extra": "^0.26.5",
    "genext2fs": "nodeos/genext2fs",
    "genfatfs": "nodeos/genfatfs",
    "minimist": "^1.2.0",
    "nodeos-cross-toolchain": "NodeOS/nodeos-cross-toolchain",
    "qemu": "nodeos/qemu#raspi",
    "suppose": "^0.5.1",
    "tar-stream": "piranna/tar-stream",
    "cpio-stream": "1.2.3"
  },
  "devDependencies": {
    "publish-release": "piranna/publish-release"
  },
  "engines": {
    "node": "^0.12.0"
  }
}
