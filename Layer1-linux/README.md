# NodeOS Docker Layer-1 Image


```
sudo name=layer1 ./build
```

Layer-1 contains all the shared libraries required by Linux.
The included script builds the layer from whatever system it's run from.
Currently the only tested system is **Ubuntu 12.04 LTS**.

